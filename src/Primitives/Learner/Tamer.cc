/**
 * @file
 * @author Thaddeus Diamond <diamond@cs.yale.edu>
 * @version 0.1
 *
 * @section DESCRIPTION
 *
 * This is the implementation for the TAMER framework for reinforcement learning
 */

#include "Learner/Tamer.h"

bool Tamer::Setup(Object* objects, size_t objects_len) {
  return true;
}

bool Tamer::LoadComparators(Key* tables, size_t tables_len) {
  return true;
}

bool Tamer::Begin(Primitive* prim, Motor* motors, size_t motors_len) {
  return true;
}

bool Tamer::UnloadData(PostProcessor* post_proc) {
  return true;
}
